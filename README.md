# run the file anywhere with --install and it will generate the necessary files. Must run hdmi-wol --sync while tv display is active.
# script will refresh anytime an hdmi port is unplugged/replugged, resyncing and waking tv.
# will auto wake tv on system start. combined with wake on usb/bluetooth controller, can near perfectly mimic industry console
# experience. only caveat is needing to enable remote control in samsung tv and clicking accept when prompted for remote control.
# experience is quite natural as a first time thing, then is automatic going forward. With the way the script is generated, as
# long as the script is installed the system should dynamically adjust to everyday activity without the need for a terminal.
